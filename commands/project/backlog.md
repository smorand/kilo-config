Use the Task tool with `add-backlog` subagent to quickly add an idea to the project backlog (specs/BACKLOG.md). The agent checks for duplicates and inconsistencies with current implementation before adding.

Pass any arguments provided as the idea to add: $ARGUMENTS
